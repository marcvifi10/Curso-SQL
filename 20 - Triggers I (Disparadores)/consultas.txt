CREATE TABLE PRODUCTOS3 (CODIGO VARCHAR(20), NOMBRE VARCHAR(20), PRECIO INT(4), INSERTADO DATETIME)

CREATE TRIGGER PRODUCTOS3_EVENTO1 AFTER INSERT ON PRODUCTOS3 FOR EACH ROW INSERT INTO REG_PRODUCTOS(CODIGO, NOMBRE, PRECIO, INSERTADO) VALUES(NEW.CODIGO, NEW.NOMBRE, NEW.PRECIO, NOW())

